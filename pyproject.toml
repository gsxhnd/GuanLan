[project]
name = "GuanLan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "yfinance>=1.4.1",
    "duckdb>=1.1.0",
    "pandas>=2.0.0",
    "grpcio>=1.70.0",
    "protobuf>=5.29.0",
    "googleapis-common-protos>=1.66.0",
]

# Python 能力：quant/crawler（无状态行情）、quant/ml（训练读 Parquet 快照、推理 gRPC）
[tool.uv]
package = false
