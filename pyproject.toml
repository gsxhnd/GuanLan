[project]
name = "GuanLan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "yfinance>=1.4.1",
    "duckdb>=1.1.0",
    "pandas>=2.0.0",
]

# Python 能力按服务划分，见 services/daily_data/（日频数据）、后续 inference/、training/
[tool.uv]
package = false
